name: CI

on:
  push:
    branches: [main]
  pull_request:

# 同一分支连续 push(如 deploy 脚本逐文件提交)只保留最新 run, 取消较早的,
# 避免每次文件提交都触发满预算 CI -> 减少冗余运行/失败通知刷屏
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# 轻量主 CI: 全部 22 套件 (协议 + 静态 + jsdom 交互), 无需真实浏览器
# Playwright E2E 在独立的 e2e.yml (手动触发) — 不占用常规预算
jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Python deps (websockets)
        run: pip install websockets

      - name: Build Go binary
        run: |
          cd impl_go
          go build -o ../bin/wxlike-go .

      - name: Install frontend deps (eslint + jsdom)
        run: npm ci --omit=dev
        working-directory: tests/frontend

      - name: Run all tests (22 suites)
        run: bash tests/run_all.sh 8081 "$(pwd)" "$(which python3)"
        timeout-minutes: 12
